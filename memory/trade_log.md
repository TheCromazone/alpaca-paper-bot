# Trade log

_Append-only. Every executed (or dry-run) order placed by an LLM routine lands
here as one row, oldest first. The `place_buy` and `place_sell` tool handlers
are the only legitimate writers — the model cannot write this file directly._

_No trades yet._
